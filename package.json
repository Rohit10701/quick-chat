{
  "name": "quick-chat",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "docker-compose -f docker-compose.yaml up -d && turbo run dev",
    "build": "turbo run build",
    "docker:down": "docker-compose -f docker-compose.yaml down"
  },
  "devDependencies": {
    "turbo": "^2.0.9"
  },
  "packageManager": "yarn@1.22.22"
}
